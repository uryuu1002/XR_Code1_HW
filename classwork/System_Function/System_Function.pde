void setup() {
  size(500, 500);
}

void draw() {
background(0);
  //if (mousePressed) {
  //  background(255);
  //} else {
  //  background(0);
  //}
}

// system function (event)
// keyPressed() - only called on the initial key press
// mousePressed() - only called on the initial mouse press
void mousePressed(){
background(255);

}
